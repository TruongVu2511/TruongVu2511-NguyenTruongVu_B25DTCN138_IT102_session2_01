#include <stdio.h>
#include <math.h>

int main() {
    int a = 3, b = 4, c = 2;
    double S;

    S = sqrt(a * a + b * b) / (c + 1.0) + (double)(a * b) / c - sqrt(fabs(a - b) + c * c);

    
}
